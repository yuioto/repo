- name: Download tat
    run: sudo pacman -Syu base-devel
         cd ~
         git clone https://aur.archlinux.org/visual-studio-code-bin.git 

- name: Arch Linux PKGBUILD builder action
  # You may pin to the exact commit or the version.
  # uses: 2m/arch-pkgbuild-builder@153f46bb4c3aea1d723a3f2651c867de46658181
  uses: 2m/arch-pkgbuild-builder@v1.18
  with:
    # Path to DIRECTORY where the PKGBUILD file is. Assumes the directory is the name of package, ie /path/to/pkgname/
    pkgname: ~/visual-studio-code-bin/
    # Validation target. Can be one of: "pkgbuild", "srcinfo", "run"
    target: # default is pkgbuild
    # Command to run after package installation. Used when target=run
    command: # optional
    # Turns debugging on
    debug: # optional
